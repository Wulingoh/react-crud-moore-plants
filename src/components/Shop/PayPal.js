import { Button } from "@mui/material";

export const PayPal = () => {
  return (
    <Button disabled variant="outlined" sx={{ mt: 1 }}>
      PayPal removed — use Cash on Pick Up
    </Button>
  );
};
